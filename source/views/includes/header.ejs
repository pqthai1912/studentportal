<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="../stylesheets/style.css">
    <title>Social DTH TDTU</title>
    <script src="https://kit.fontawesome.com/ef7e2b893b.js" crossorigin="anonymous"></script>
    <script src="http://ajax.googleapis.com/ajax/libs/jquery/1.7.1/jquery.min.js" type="text/javascript"></script>
    <script src="https://cdn.jsdelivr.net/jquery.inview/0.2/jquery.inview.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/socket.io/4.4.0/socket.io.js"></script> 
    <script src="https://cdnjs.cloudflare.com/ajax/libs/notify/0.4.2/notify.js"></script>

</head>

<body>
    <nav class="navbar">
        <div class="nav-left"><a href="/<%=(!user.googleId) ? 'department' : ''%>"><img class="logo" src="../images/images/logoTDT.png" alt=""></a>
        </div>
        <div class="nav-right">
            <div class="search-box">
                <!-- <img src="../images/images/search.png" alt="">
                <input type="text" placeholder="Search"> -->
            </div>
            <div class="profile-image online" onclick="UserSettingToggle()">
                <img src="<%=user.image%>" alt="">
            </div>

        </div>
        <div class="user-settings">
            <div class="profile-darkButton">
                <div class="user-profile">
                    <img src="<%=user.image%>" alt="">
                    <div>
                        <p> <%=user.name%></p>
                        <% if(!user.googleId){ %>
                            <a href="/department/profile/<%=user._id%>">Trang cá nhân</a>
                        <% }else{ %>
                            <a href="/profile/<%=user._id%>">Trang cá nhân</a>
                        <% } %>
                    </div>
                </div>
                <div id="dark-button" onclick="darkModeON()">
                    <span></span>
                </div>
            </div>
            <hr>
            <div class="settings-links">
                <img src="../images/images/logout.png" alt="" class="settings-icon">
                <a href="/user/logout">Logout <img src="../images/images/arrow.png" alt=""></a>
            </div>

        </div>
    </nav>

    <!-- content-area------------ -->
   
    <div class="container">
        <div class="left-sidebar">
                        
            <div class="shortcut-links">
                <% if(user.role !== 2){%>
                    <h2 class="page-auth">Trang quản lý</h2>
                    <% if(user.role === 0){ %>
                        <a href="/admin/account"> <img src="../images/images/admin.png" alt="">Admin</a>

                    <%}else{ %>
                        <a href="/admin/notification/manager"> <img src="../images/images/department.png" alt="">Phòng/Khoa</a>
                    <%} %>
                <% }else{ %>
                        <img id="poster" src="../images/poster_ads.jpg" alt="" width="350" height="100%">
                <% } %>
            </div>
        </div>
    